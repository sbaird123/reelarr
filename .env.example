# Everything here is optional — the Settings UI at /settings is the source
# of truth once the app is running. These values only seed defaults on
# first boot.

PORT=3000
# CONFIG_DIR=.

TMDB_API_KEY=
